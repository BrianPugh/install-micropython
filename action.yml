name: "Install MicroPython"
description: "Build and install MicroPython"
author: "Brian Pugh <bnp117@gmail.com>"
branding:
  icon: "package"
  color: "green"
inputs:
  repository:
    description: "Micropython Git Repository"
    required: false
    default: "https://github.com/micropython/micropython"
  reference:
    description: "Specific branch, tag, or commit SHA to checkout"
    required: false
  cflags:
    description: "Extra cflags to pass to micropython at compile-time."
    required: false
  submodules:
    description: "Initialize MicroPython submodules so MPY_DIR is fully usable (e.g. natmod builds), even on cache hits. Set to false for faster cached runs when only the binaries are needed. Submodules are always initialized when building from source."
    required: false
    default: "true"
outputs:
  cache-hit:
    description: "Whether the binaries were restored from cache ('true'/'false')."
  sha:
    description: "The resolved MicroPython commit SHA that was installed."
runs:
  using: "node20"
  main: "dist/index.js"
